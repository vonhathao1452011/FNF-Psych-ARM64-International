	<!-- ====================================================================== -->
	<!-- GLOBAL ANDROID HIGH-PERFORMANCE & SHARP HD GRAPHICS CONFIGURATION      -->
	<!-- ====================================================================== -->
	<!-- Enforces 64-bit native ARM execution for Samsung Exynos processors -->
	<architecture name="arm64" />

	<!-- Hardware acceleration and high-performance memory allocator flags -->
	<haxedef name="HXCPP_GC_BIG_BLOCKS" />
	<haxedef name="no_precompiled_headers" />
	<haxedef name="lime_opengl" />
	<haxedef name="disable_preloader_assets" />

	<!-- Disables mobile blur filter, keeping original PC textures crisp & HD -->
	<haxedef name="openfl_disable_graphics_upscaling" />
	<!-- ====================================================================== -->
